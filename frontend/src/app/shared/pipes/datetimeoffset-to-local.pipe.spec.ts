import { DatetimeoffsetToLocalPipe } from './datetimeoffset-to-local.pipe';

describe('DatetimeoffsetToLocalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeoffsetToLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
